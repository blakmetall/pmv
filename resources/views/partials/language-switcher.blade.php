<div 
    class="app-language-switcher" 
    label="{{ __('LANGUAGE') }}"
    spanishLabel = "{{ __('Spanish') }}"
    spanishUrl = "{{ route('language.update', ['es']) }}"
    englishLabel = "{{ __('English') }}"
    englishUrl = "{{ route('language.update', ['en']) }}"
    >
</div>